I’m presenting a multimodal search system that uses two agent patterns:
1) Planning agent pattern
2) Reflection-based agent pattern


Let's look at the first one which is the planning agent pattern in the Scenario of
A Content Brief Workflow. Let me share a condtent brief document for reviww 

This is a customer brief with full narrative details describing the desired assets:
- specific image/video characteristics
- lighting and mood
- quality expectations
- technical constraints
- exclusions and usage preferences

I’ll scroll through the brief while I explain that this is the source of truth for what the customer actually wants.
which is currently read by creative analysts who are humans who process the criteria and then run searches, applies fileters to find right content. 
In agentic disvovery system,  I’ll just upload this brief file and run a search request based on it.

With a file uploaded, the app runs structured analysis first similar to what creative analyst would do.
and from here on it's so much powerful than human workflow because it has access to the tools and it can decide the type o tool to chose, reflect and iterate. 

For example, in this scenrio, 

The Project Manager / planning layer extracts and structures:
- intent and domain context
- lexical and semantic query candidates
- visual style, mood, quality, and technical constraints
- filters, exclusions, and category hints

and there is 
1) intent analysis
2) query generation
3) filter generation
4) search execution happening in steps. 

This is the planning pattern end-to-end:
understand the brief first, build the plan, then retrieve assets aligned to that plan.


Let's move to secneio2 2: Multimodal Search + Tool Calling

Now I’ll demo multimodal search directly from text.
I’ll run this query:
`assets of nyc buildings specifically empire state building during winter`

 the system returns both videos and images that match the query.

Then I’ll run follow-up refinements:
`show popular images only`
`show generated images only`

I’ll briefly explain agent tool calling here:
- the agent calls image and video tools
- it merges and filters results based on the latest user instruction


Scenario 3: Reflection Pattern

Now I’ll run:
`assets of aerial view of atlanta city night sky`

After baseline retrieval, I’ll ask:
`respond with best matches for the same query`

I’ll briefly explain reflection:
the system gets initial results first, then performs a reflection/rerank pass to surface stronger final matches.


Closing

Scenario 2 showed planning before retrieval from a brief.
Scenario 1 showed multimodal retrieval and iterative tool-based refinement.
Scenario 3 showed reflection after retrieval to improve final relevance.
